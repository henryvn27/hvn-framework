# Draft Ecosystem Issues

Use this folder when the automation identifies an `Adopt now` finding but cannot open a draft issue directly in the issue tracker.

Create one Markdown file per finding and avoid duplicates when a substantially similar open or already-tracked issue exists.
