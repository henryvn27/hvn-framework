name: Evals

on:
  push:
  pull_request:

jobs:
  evals:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check starter eval set and contracts
        run: ./scripts/check-reliability.sh
