# Maker Checker QA

Use maker-checker when implementation and validation should stay separate.

The implementation worker builds.
The checker reviews or tests independently.
The parent owns the final synthesis.
