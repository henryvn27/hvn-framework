# Plugin Compatibility Block Example

An otherwise valid update is held back because a pinned plugin contract is not yet marked compatible, so ORCA stays in prompt or defer mode instead of auto-apply.
