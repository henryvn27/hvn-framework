# Install Failure Recovery Example

A user hits a broken local install target, removes only `./.orca-framework`, reinstalls it, reruns verification, and avoids redoing unrelated auth or plugin setup.
