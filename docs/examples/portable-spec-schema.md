# Example Portable Spec Schema

See [schema/examples/spec-v1.json](../../schema/examples/spec-v1.json) for a machine-readable spec payload that matches the v1 spec schema.

## Why It Matters

- structure is explicit
- version is visible
- a mapper or validator can consume it without scraping Markdown

The Markdown spec still remains the primary human review artifact.
