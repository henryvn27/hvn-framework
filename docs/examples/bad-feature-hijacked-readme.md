# Bad Feature-Hijacked README

A bad README reads like the whole project is mainly about whichever subsystem was added most recently, even when that subsystem is only one support layer inside a larger framework.
