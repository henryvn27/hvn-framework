# Post-Spec Guidance

```text
Completed: spec contract is written.

Recommended next action: create the milestone plan and verification gates.

Why: implementation should start from a bounded slice, not the whole spec.

Avoid: starting build while acceptance criteria are still fuzzy.
```
