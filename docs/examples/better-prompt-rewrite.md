# Better Prompt Rewrite

Instead of:

`make this better`

ORCA-HVN might say:

`Try saying this next time: "Refactor the onboarding copy in /app/onboarding.tsx to sound more direct and reduce repeated phrases. Keep the structure the same and return the exact diff."`
