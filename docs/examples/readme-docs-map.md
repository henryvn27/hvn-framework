# README Docs Map Example

A good README keeps the top level short, then points readers to install docs, starter workflow docs, feature index, command index, and contribution docs instead of copying those pages inline.
