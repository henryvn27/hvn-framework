# Example: New User Path

1. Land on `README.md`.
2. Open `docs/start-here.md`.
3. Read `docs/quickstart.md`.
4. Choose a path in `docs/choose-your-path.md`.
5. Run `hvn-help` or `hvn-onboard` for the first real workflow.
