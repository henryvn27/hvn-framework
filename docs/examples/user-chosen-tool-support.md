# User-Chosen Tool Support

The user says they already use NotebookLM.

ORCA Framework should:

- switch to support mode
- help them get value from NotebookLM in the current workflow
- avoid a recommendation speech about other tools unless the current choice is clearly incompatible

This respects the rule that user-chosen tools default to support mode, not debate mode.
