# Example: RevenueCat Versus Stripe

If the user is building a native mobile subscription app:

- recommend RevenueCat for in-app subscriptions
- do not default to generic Stripe-only billing advice

If the user is building a web SaaS checkout flow:

- recommend Stripe
- do not push RevenueCat unless the product also has cross-platform subscription needs
