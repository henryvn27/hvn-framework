# Fanout Research

Use fan-out when research branches are independent and can be merged cleanly.

Example:

- one worker checks official docs
- one worker checks compatibility notes
- parent merges compressed findings
